# Retired pages -> closest live equivalent. These must sit above the SPA
# fallback: Netlify matches _redirects rules in order, and the catch-all below
# would otherwise answer with index.html and a 200 (a soft 404).
/services/move-in-assembly    /services/furniture-assembly   301!

# SPA fallback for client-only routes (admin, portal, dynamic paths)
# Static HTML files generated by vite-react-ssg are served directly by Netlify
/*    /index.html   200
